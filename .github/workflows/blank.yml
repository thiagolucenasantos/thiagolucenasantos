Skip to content
Formandodev
/
Formandodev

Type / to search

Code
Issues
2
Pull requests
Actions
Projects
Security
Insights
Commit
Cobrinha
 main
@Formandodev
Formandodev committed on Mar 8, 2022 
1 parent 4d43102
commit 89af109
Showing 1 changed file with 47 additions and 0 deletions.
 47 changes: 47 additions & 0 deletions47  
.github/workflows/blank.yml
@@ -0,0 +1,47 @@
# Nome da Actions:  
name: Snake Game

# Controlador do tempo que sera feito a atualização dos arquivos.
on:
  schedule:
      # Será atualizado a cada 5 horas.
    - cron: "0 */5 * * *"

# Permite executar na na lista de Actions (utilizado para testes de build).
  workflow_dispatch:

# Regras
jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    # Checks repo under $GITHUB_WORKSHOP, so your job can access it
      - uses: actions/checkout@v2

    # Repositorio que será utilizado para gerar os arquivos.
      - uses: Platane/snk@master
        id: snake-gif
        with:
          github_user_name: thiagolucenasantos #Seu usuario
          gif_out_path: dist/github-contribution-grid-snake.gif
          svg_out_path: dist/github-contribution-grid-snake.svg

      - run: git status

      # Para as atualizações.
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: master
          force: true

      - uses: crazy-max/ghaction-github-pages@v2.1.3
        with:
          # the output branch we mentioned above
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

0 comments on commit 89af109
@thiagolucenasantos
 
Leave a comment
Nenhum ficheiro selecionado
Attach files by dragging & dropping, selecting or pasting them.
Footer
© 2023 GitHub, Inc.
Footer navigation
Terms
Privacy
Security
Status
Docs
Contact GitHub
Pricing
API
Training
Blog
About
Formandodev/README.md at main · Formandodev/Formandodev 
